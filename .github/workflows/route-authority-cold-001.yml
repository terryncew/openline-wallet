name: ROUTE-AUTHORITY-COLD-001

on:
  push:
    branches: [main, "proof/**"]
  pull_request:

permissions:
  contents: read

jobs:
  route-authority:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: python -m pip install -e .
      - name: Existing Wallet unit suite
        run: python -m unittest discover -s tests -v
      - name: Reproduce and verify ROUTE-AUTHORITY-COLD-001
        run: |
          rm -rf route-authority-cold-artifacts
          python proofs/route-authority-cold-001/run.py --output route-authority-cold-artifacts
          python proofs/route-authority-cold-001/verify.py route-authority-cold-artifacts
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: route-authority-cold-${{ matrix.python-version }}
          path: route-authority-cold-artifacts/
          if-no-files-found: warn
