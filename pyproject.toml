[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "openline-wallet"
version = "0.1.0"
description = "User-owned authority continuity for AI agents"
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = [{name = "Terrynce White"}]
dependencies = ["cryptography>=41"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]

[project.optional-dependencies]
mcp = ["mcp>=2,<3"]

[project.scripts]
openline-wallet = "openline_wallet.cli:main"
openline-wallet-gate = "openline_wallet.gate_http:main"
openline-wallet-mcp = "openline_wallet.mcp_bridge:main"
openline-wallet-platform-exit = "openline_wallet.platform_exit_live:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
